'use strict';

/**
 * Given a DOM element, appends all tasks stored in a given array to that
 * element.
 * @param {Array} arr - The array of tasks
 * @param {*} selector - The selector of the element to append to.
 * @returns {Boolean} - True if completed.
 */
exports.insertAllTasks function(arr, selector) {

  for (let i = 0; i < list.tasks.)
}